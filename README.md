# Säkra besiktningar website

Development and deploy scripts in [package.json](./package.json).

Generate images statically for Nuxt Image with `nuxt generate`. Then copy/move _ipx folder to /public.
